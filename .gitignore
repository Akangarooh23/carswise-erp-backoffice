node_modules
dist
build
.env
# Y cualquier copia suya: una .env.bak lleva los mismos secretos dentro.
.env.*
!.env.example
.env.local
coverage
.DS_Store
.vercel

# Herramienta de revision visual: lleva credenciales dentro y hace capturas
# del ERP con datos reales de clientes. Ninguna de las dos cosas va al repo.
recorre-erp.js
capturas-erp/

# Cache del compilador: cambia en cada compilacion y ensucia el git status.
*.tsbuildinfo
