# ── Shared ───────────────────────────────────────────────────────────────────
NODE_ENV=development
PORT=4000

# ── Backend auth ─────────────────────────────────────────────────────────────
# JWT_SECRET must be at least 16 characters. Generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_to_a_long_random_string_minimum_32_chars

# Staff passwords — each must be at least 8 characters. NO default fallbacks.
ERP_ADMIN_PASSWORD=
ERP_SUPPORT_PASSWORD=
ERP_OPS_PASSWORD=
ERP_SALES_PASSWORD=

# ── Database ─────────────────────────────────────────────────────────────────
DATABASE_URL=postgresql://user:password@host:5432/carswise_erp

# ── CORS ─────────────────────────────────────────────────────────────────────
CORS_ORIGIN=http://localhost:5174

# ── Frontend (Vite) ───────────────────────────────────────────────────────────
# Not needed if using Vite proxy (recommended in dev)
# VITE_API_URL=http://localhost:4000/api

# ── Email (Resend) ───────────────────────────────────────────────────────────
# Get your key at resend.com/api-keys
RESEND_API_KEY=
# "From" address shown to clients (must be a verified domain in Resend)
RESEND_FROM_EMAIL=CarsWise <noreply@carswiseai.com>
# DEV ONLY: if set, ALL outgoing emails are redirected to this address instead
# of the real recipient. Remove or leave empty in production.
RESEND_TEST_EMAIL=

# ── Notifications ─────────────────────────────────────────────────────────────
# Internal email that receives alerts on new leads and status changes
INTERNAL_LEADS_EMAIL=

# ── Staff email format (for reference) ───────────────────────────────────────
# admin@carswise.es      → ERP_ADMIN_PASSWORD
# support@carswise.es   → ERP_SUPPORT_PASSWORD
# ops@carswise.es       → ERP_OPS_PASSWORD
# sales@carswise.es     → ERP_SALES_PASSWORD
